import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["framerusercontent.com", "premierpainters.ca", "randomuser.me","images.unsplash.com"],
  },
};

export default nextConfig;
